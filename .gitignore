# OS
.DS_Store
Thumbs.db
desktop.ini

# Editores
.vscode/
.idea/

# Vercel (gerado localmente pelo `vercel link` / `vercel dev`)
.vercel

# Variáveis de ambiente / segredos
.env
.env.local
.env*.local

# Node (caso alguma ferramenta de build seja adicionada no futuro)
node_modules/
npm-debug.log*
